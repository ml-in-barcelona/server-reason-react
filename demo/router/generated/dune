(rule
 (targets Router.re Router.rei RouterRegistry.re)
 (deps Routes.re)
 (action
  (progn
   (with-stdout-to
    Router.re
    (pipe-outputs
     (run %{bin:refmt} --parse re --print ml %{dep:Routes.re})
     (run
      %{bin:server-reason-react.router-gen}
      --mode
      handles
      --source
      %{dep:Routes.re})
     (run %{bin:refmt} --parse ml --print re)))
   (with-stdout-to
    Router.rei
    (pipe-outputs
     (run %{bin:refmt} --parse re --print ml %{dep:Routes.re})
     (run
      %{bin:server-reason-react.router-gen}
      --mode
      interface
      --source
      %{dep:Routes.re})
     (run %{bin:refmt} --parse ml --print re --interface true)))
   (with-stdout-to
    RouterRegistry.re
    (pipe-outputs
     (run %{bin:refmt} --parse re --print ml %{dep:Routes.re})
     (run
      %{bin:server-reason-react.router-gen}
      --mode
      registry
      --source
      %{dep:Routes.re})
     (run %{bin:refmt} --parse ml --print re))))))
