(library 
 (name cross_test_js)
 (modes melange)
 (modules :standard)
 (libraries reason-react)
 (preprocess
  (pps melange.ppx reason-react-ppx server-reason-react.browser_ppx)))

(melange.emit
  (target main)
  (alias js)
  (module_systems es6)
  (modules)
  (libraries cross_test_js))

;(test
; (name cross_test_js)
; (modules cross_test_js)
; (libraries
;   cross_test_js_lib
;   alcotest
;   fmt))
;
;(copy_files# "../native/shared/*{.ml,.re}")
;(copy_files# "../native/shared/cross_test.ml")
(copy_files# "../native/shared/test_context.re")
