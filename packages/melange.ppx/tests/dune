(cram
 (package server-reason-react)
 ; Some tests print a file error (including file path) which fails on windows, since it's expected to run on a unix system
 (enabled_if
  (<> %{os_type} Win32))
 (deps %{bin:ocamlformat} standalone.exe))

(executable
 (name standalone)
 (libraries ppxlib melange_native_ppx))
